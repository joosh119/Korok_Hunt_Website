import '@aws-amplify/core/internals/utils';
import { assertValidationError } from '../errors/assertValidatonError.mjs';
import { RestApiValidationErrorCode } from '../errors/validation.mjs';

// Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
// SPDX-License-Identifier: Apache-2.0
/**
 * @internal
 */
const resolveCredentials = async (amplify) => {
    const { credentials } = await amplify.Auth.fetchAuthSession();
    assertValidationError(!!credentials && !!credentials.accessKeyId && !!credentials.secretAccessKey, RestApiValidationErrorCode.NoCredentials);
    return credentials;
};

export { resolveCredentials };
//# sourceMappingURL=resolveCredentials.mjs.map
