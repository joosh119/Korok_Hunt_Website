import { OAuthConfig } from '@aws-amplify/core';
import { DefaultOAuthStore } from '../utils/signInWithRedirectStore';
import { SignInWithRedirectInput } from '../types';
/**
 * Signs in a user with OAuth. Redirects the application to an Identity Provider.
 *
 * @param input - The SignInWithRedirectInput object, if empty it will redirect to Cognito HostedUI
 *
 * @throws AuthTokenConfigException - Thrown when the userpool config is invalid.
 * @throws OAuthNotConfigureException - Thrown when the oauth config is invalid.
 */
export declare function signInWithRedirect(input?: SignInWithRedirectInput): Promise<void>;
export declare const store: DefaultOAuthStore;
export declare function oauthSignIn({ oauthConfig, provider, clientId, customState, preferPrivateSession, }: {
    oauthConfig: OAuthConfig;
    provider: string;
    clientId: string;
    customState?: string;
    preferPrivateSession?: boolean;
}): Promise<void>;
export declare function parseRedirectURL(): Promise<void>;
